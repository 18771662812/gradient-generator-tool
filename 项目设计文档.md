# 角色定义
你是一名全栈工程师，擅长 Vue3 + Flask 前后端分离开发及 Docker 容器化部署。请严格按照以下规格，完整实现一个「CSS 渐变生成与预览工具」项目，输出所有文件的完整代码，不得省略任何内容。

---

# 项目背景与目标

这是一个高校数据库课程的结课项目，需满足以下硬性要求：
1. 后端连接 OpenGauss 数据库（PostgreSQL 协议兼容）进行读写
2. 前后端分离，分别打包为 Docker 镜像
3. 每个服务都有对应的 Dockerfile
4. 功能完整，界面完整，有前后台交互逻辑
5. 具备用户注册/登录系统，以及收藏功能

---

# 技术栈（严格遵守，不得替换）

## 前端
- Vue 3 + Vite（组合式 API / script setup 语法）
- Pinia（状态管理，含 useAuthStore 和 useGradientStore）
- Vue Router 4（含路由守卫，未登录跳转 /login）
- Axios（HTTP 请求，请求拦截器自动附加 Authorization: Bearer ）
- 不使用任何 UI 组件库，自行编写 CSS

## 后端
- Python 3.11 + Flask 3.x
- Flask-CORS
- psycopg2-binary（连接 OpenGauss/PostgreSQL）
- PyJWT（JWT 签发与验证）
- bcrypt（密码哈希）
- 所有配置通过环境变量注入，不硬编码

## 数据库
- OpenGauss / PostgreSQL（psycopg2 连接）
- 初始化 SQL 脚本单独提供

## 容器化
- 前端：多阶段构建（node:18-alpine build → nginx:alpine serve）
- 后端：python:3.11-slim
- 完整 docker-compose.yml（含 opengauss 服务）
- nginx.conf（含 /api/ 反向代理）

---

# 数据库设计

## 表1：users（用户）
```sql
CREATE TABLE IF NOT EXISTS users (
    id          SERIAL PRIMARY KEY,
    username    VARCHAR(50)  UNIQUE NOT NULL,
    password    VARCHAR(255) NOT NULL,  -- bcrypt hash
    created_at  TIMESTAMP    NOT NULL DEFAULT NOW()
);
```

## 表2：gradients（渐变方案）
```sql
CREATE TABLE IF NOT EXISTS gradients (
    id          SERIAL PRIMARY KEY,
    user_id     INTEGER      NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    name        VARCHAR(100) NOT NULL DEFAULT '未命名方案',
    type        VARCHAR(10)  NOT NULL CHECK (type IN ('linear', 'radial')),
    angle       INTEGER      NOT NULL DEFAULT 90,
    stops       JSONB        NOT NULL,
    css_value   TEXT         NOT NULL,
    is_public   BOOLEAN      NOT NULL DEFAULT false,
    created_at  TIMESTAMP    NOT NULL DEFAULT NOW(),
    updated_at  TIMESTAMP    NOT NULL DEFAULT NOW()
);
```
stops 字段结构示例：
```json
[{"color": "#ff6b6b", "position": 0}, {"color": "#4ecdc4", "position": 100}]
```

## 表3：favorites（收藏，多对多）
```sql
CREATE TABLE IF NOT EXISTS favorites (
    user_id     INTEGER NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    gradient_id INTEGER NOT NULL REFERENCES gradients(id) ON DELETE CASCADE,
    created_at  TIMESTAMP NOT NULL DEFAULT NOW(),
    PRIMARY KEY (user_id, gradient_id)
);
```

---

# 认证规范

## JWT
- 签发时包含：{ "user_id": int, "username": string, "exp": now+7天 }
- 密钥通过环境变量 JWT_SECRET 注入
- 前端收到 token 后存入 localStorage，Pinia useAuthStore 中维护 user 状态
- Axios 请求拦截器：若 localStorage 有 token，自动添加 Authorization: Bearer 

## 后端鉴权装饰器
实现 @require_auth 装饰器，验证失败返回 { "error": "未授权" }，HTTP 401。
需要登录的接口一律使用此装饰器。

## 路由守卫
以下路由需要登录，未登录跳转 /login：
- /editor
- /my-gradients
- /favorites

以下路由无需登录：
- /（首页，重定向到 /plaza）
- /plaza（公共广场）
- /login
- /register

---

# 后端 API 规范

Base URL: /api
所有请求和响应均为 JSON。

## 认证接口（无需 token）

### POST /api/auth/register
请求体：{ "username": string, "password": string }
验证：username 3-20位字母数字，password 至少6位
响应：{ "token": string, "user": { "id", "username" } }
错误：用户名已存在返回 409

### POST /api/auth/login
请求体：{ "username": string, "password": string }
响应：{ "token": string, "user": { "id", "username" } }
错误：用户名或密码错误返回 401

### GET /api/auth/me（需要 token）
响应：{ "user": { "id", "username", "created_at" } }

## 渐变方案接口

### GET /api/gradients/my（需要 token）
返回当前登录用户的所有方案，按 created_at 降序
响应：{ "data": [ gradient对象 ], "total": number }

### POST /api/gradients（需要 token）
创建方案，user_id 从 token 中取，不接受客户端传入
请求体：{ "name", "type", "angle", "stops", "css_value", "is_public" }
响应：{ "data": gradient对象, "message": "保存成功" }

### PUT /api/gradients/（需要 token）
只能修改自己的方案，字段：name、is_public
请求体：{ "name"?: string, "is_public"?: boolean }
响应：{ "data": gradient对象, "message": "更新成功" }
错误：非本人操作返回 403

### DELETE /api/gradients/（需要 token）
只能删除自己的方案
响应：{ "message": "删除成功" }
错误：非本人操作返回 403

### GET /api/gradients/（无需 token）
获取单个方案，若 is_public=false 且非本人，返回 403

## 公共广场接口

### GET /api/plaza
返回所有 is_public=true 的方案，按 created_at 降序，关联返回作者 username
响应：{ "data": [ { ...gradient字段, "author": username, "is_favorited": bool } ], "total": number }
注意：若请求携带有效 token，is_favorited 字段反映当前用户是否已收藏；未登录则 is_favorited 均为 false

## 收藏接口（均需要 token）

### GET /api/favorites
返回当前用户收藏的所有方案（关联 gradients 和 users 表）
响应：{ "data": [ { ...gradient字段, "author": username } ], "total": number }

### POST /api/favorites/
收藏指定方案（只能收藏 is_public=true 的方案，且不能收藏自己的）
响应：{ "message": "收藏成功" }
错误：已收藏返回 409，不能收藏自己的返回 400

### DELETE /api/favorites/
取消收藏
响应：{ "message": "已取消收藏" }

所有错误统一返回：{ "error": "错误描述" }

---

# 前端功能与页面规范

## 路由结构
- /plaza           → 公共广场（默认首页，无需登录）
- /login           → 登录页
- /register        → 注册页
- /editor          → 渐变编辑器（需登录）
- /editor/:id      → 加载指定方案到编辑器（需登录）
- /my-gradients    → 我的方案库（需登录）
- /favorites       → 我的收藏（需登录）

## 顶部导航栏（所有页面共有）
- 左侧：项目名称「GradientLab」，点击跳转 /plaza
- 中间：Plaza / 编辑器 / 我的方案 / 我的收藏（路由高亮）
- 右侧：未登录显示「登录」「注册」按钮；已登录显示用户名 + 「退出」按钮

## 登录页（/login）
- 用户名 + 密码输入框
- 登录按钮，有 loading 状态
- 错误提示
- 底部「没有账号？去注册」链接

## 注册页（/register）
- 用户名 + 密码 + 确认密码输入框
- 前端校验：密码一致性、用户名格式
- 注册成功后自动登录并跳转 /editor
- 底部「已有账号？去登录」链接

## 编辑器页（/editor）

### 1. 渐变类型切换
线性渐变 / 径向渐变 Tab，实时更新预览

### 2. 角度控制（仅线性渐变）
圆盘式拖拽选择器 + 数字输入框（0-360）

### 3. 色标编辑器
- 每个色标：颜色选择器 + 位置滑块 + 删除按钮
- 支持添加色标（最少2个，最多8个）
- 支持拖拽排序

### 4. 实时预览区（3个卡片）
- 大背景块（320×180px）
- 模拟按钮（200×48px，圆角24px，白色文字「点击按钮」）
- 模拟卡片（240×140px，圆角12px）

### 5. CSS 输出 & 一键复制
展示完整 CSS，复制后按钮2秒内显示「已复制！」

### 6. 保存面板
- 方案名称输入
- 「公开到广场」开关（toggle，控制 is_public）
- 保存按钮

## 我的方案库（/my-gradients）
- 网格布局，每张卡片：渐变预览 + 名称 + 时间 + 公开状态标签
- 点击卡片 → 跳转 /editor/:id
- 重命名（行内编辑）、切换公开/私有、删除
- 空状态提示

## 公共广场（/plaza）
- 瀑布流或网格布局，展示所有公开方案
- 每张卡片：渐变预览 + 名称 + 作者名 + 收藏数
- 已登录用户可点击「收藏」按钮（已收藏显示实心，再次点击取消）
- 点击卡片可加载到编辑器（需登录）
- 未登录点击收藏/加载时，提示「请先登录」并跳转 /login

## 我的收藏（/favorites）
- 展示当前用户收藏的所有方案
- 每张卡片：渐变预览 + 名称 + 作者名 + 取消收藏按钮
- 点击卡片加载到编辑器
- 空状态提示

---

# 文件结构（必须完整输出）

```
gradient-tool/
├── docker-compose.yml
├── init.sql
├── backend/
    ├── Dockerfile
    ├── requirements.txt
    ├── run.py                  # 启动入口，只做 init + app.run
    │
    └── app/
        ├── __init__.py         # create_app() 工厂函数，注册蓝图、CORS
        ├── config.py           # 所有环境变量读取，集中管理
        │
        ├── database/
        │   ├── __init__.py
        │   └── pool.py         # 连接池初始化、get/return 连接
        │
        ├── models/             # 纯数据库操作，无业务逻辑
        │   ├── __init__.py
        │   ├── user.py         # find_by_username, create_user
        │   ├── gradient.py     # create, find_by_id, list_by_user, list_public, update, delete
        │   └── favorite.py     # add, remove, exists, list_by_user
        │
        ├── services/           # 业务逻辑，调用 models，不直接操作 DB
        │   ├── __init__.py
        │   ├── auth_service.py   # register, login, decode_token 逻辑
        │   └── gradient_service.py  # 权限校验 + 调用 model
        │
        ├── routes/             # 蓝图，只做参数解析和响应组装
        │   ├── __init__.py
        │   ├── auth.py         # /api/auth/*
        │   ├── gradients.py    # /api/gradients/*
        │   ├── plaza.py        # /api/plaza
        │   └── favorites.py    # /api/favorites/*
        │
        └── middleware/
            ├── __init__.py
            └── auth.py         # require_auth 装饰器, get_optional_auth
├── frontend/
│   ├── Dockerfile
│   ├── nginx.conf
│   ├── package.json
│   ├── vite.config.js
│   ├── index.html
│   └── src/
│       ├── main.js
│       ├── App.vue
│       ├── router/
│       │   └── index.js          # 含路由守卫
│       ├── stores/
│       │   ├── auth.js           # useAuthStore：token、user、login、logout
│       │   └── gradient.js       # useGradientStore：当前编辑状态
│       ├── api/
│       │   └── index.js          # Axios 实例 + 请求拦截器 + 所有接口方法
│       ├── views/
│       │   ├── PlazaView.vue
│       │   ├── LoginView.vue
│       │   ├── RegisterView.vue
│       │   ├── EditorView.vue
│       │   ├── MyGradientsView.vue
│       │   └── FavoritesView.vue
│       └── components/
│           ├── NavBar.vue
│           ├── GradientCard.vue      # 通用渐变卡片（广场/收藏/我的方案复用）
│           ├── GradientPreview.vue
│           ├── ColorStopEditor.vue
│           ├── AnglePicker.vue
│           └── CSSOutput.vue
```

---

# 样式规范

- 深色主题：背景 #0f0f13，卡片 #1a1a24，边框 #2a2a3a
- 主色：#7c6aff（紫色），成功色：#4ecdc4，危险色：#ff6b6b，警告色：#f7c948
- 字体：-apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif
- 所有交互元素有 hover / active / disabled 状态
- 表单输入框统一样式，focus 时主色边框发光效果
- GradientCard 组件在广场页需展示收藏按钮，在我的方案页展示编辑/删除按钮，通过 props 控制显示哪些操作

---

# 输出规范（严格遵守）

1. 按文件结构逐文件输出完整代码
2. 每个文件用 Markdown 代码块包裹并标注路径，例如：
   ```js
   // frontend/src/main.js
   ```
3. 禁止使用任何省略：不得写「省略」「同上」「此处逻辑类似」「// ...」
4. 输出顺序：docker-compose.yml → init.sql → backend/* → frontend/*
5. app.py 需包含数据库连接重试逻辑（最多重试5次，每次间隔2秒）
6. 所有前端 API 调用需有 loading 状态和 try/catch 错误处理
7. GradientCard.vue 必须是真正的通用组件，通过 props 控制行为，不允许三个页面各自写卡片

---

# 验收标准

执行 `docker-compose up --build` 后：
- 注册账号 → 登录 → 创建渐变 → 保存（公开）→ 在广场看到 → 用另一账号登录 → 收藏 → 在收藏页查看 → 取消收藏，全流程无报错
- 未登录访问 /editor 自动跳转 /login
- 数据持久化到 OpenGauss，重启容器后数据不丢失